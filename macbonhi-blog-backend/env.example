# 环境配置
NODE_ENV=production

# API服务配置
PORT=3000

# 数据库配置
DB_HOST=db
DB_USER=root
DB_PASSWORD=root
DB_NAME=macbonhiblog

# 跨域配置
# 以逗号分隔允许的域名，生产环境下应只包含实际使用的域名
ALLOWED_ORIGINS=https://macbonhi.cn,http://macbonhi.cn

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d 